{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:lcars:events-v1",
  "title": "LCARS Events Catalogue v1",
  "description": "Structural schema for 05_data-canon/config/events.yaml (parsed YAML map). DERIVED FROM: 05_data-canon/config/events.yaml. GO-7: date 2026-05-19, statut actif, derive Lot 0bis plan-implementation.md. Validated by ex_json_schema at Fleet.EventRouter.Dispatch.init/1. Structural only: handler module *existence* is checked at runtime by the router, not here.",
  "type": "object",
  "required": ["events"],
  "additionalProperties": false,
  "properties": {
    "events": {
      "type": "object",
      "minProperties": 1,
      "description": "Map event_type -> [handler_module, ...]. Fan-out N handlers via Phoenix.PubSub.",
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*$"
      },
      "additionalProperties": {
        "type": "array",
        "minItems": 0,
        "items": {
          "type": "string",
          "pattern": "^Fleet\\.[A-Za-z][A-Za-z0-9]*(\\.[A-Za-z][A-Za-z0-9]*)*$"
        }
      }
    }
  }
}
