{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "lcars/mcp-channels-v1",
  "title": "LCARS MCP Channels Catalogue v1",
  "description": "Structural schema for 05_data-canon/config/mcp-channels.yaml (parsed YAML map). DERIVED FROM: 05_data-canon/config/mcp-channels.yaml. GO-7: date 2026-05-19, statut actif, derive Lot 1 plan-implementation.md (ring4/fleet_mcp.md §Config channels). Validated by ex_json_schema at Fleet.MCP.Server.init/1 (channel registration au boot). Structural only: topic glob semantics + transport availability are checked at runtime, not here. Strict (additionalProperties:false): cle config inconnue = fail-fast boot, coherent canon refus-defaut.",
  "type": "object",
  "required": ["channels"],
  "additionalProperties": false,
  "properties": {
    "channels": {
      "type": "object",
      "minProperties": 1,
      "description": "Map channel-name -> spec. MVP canon = fleet-control + fleet-forge.",
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$"
      },
      "additionalProperties": {
        "type": "object",
        "required": ["description", "transport", "sub_topics"],
        "additionalProperties": false,
        "properties": {
          "description": {
            "type": "string",
            "minLength": 1
          },
          "transport": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "description": "Transports exposes pour ce channel. native_beam = substrat interne umbrella uniquement (INTERDIT cote pod, ADR-C 5 zeros) ; les channels pod-facing canon = stdio + http_sse.",
            "items": {
              "type": "string",
              "enum": ["stdio", "http_sse", "native_beam"]
            }
          },
          "sub_topics": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "description": "Glob patterns de sous-topics (ex: fleet-control.memory.*). Semantique glob resolue runtime.",
            "items": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      }
    }
  }
}
