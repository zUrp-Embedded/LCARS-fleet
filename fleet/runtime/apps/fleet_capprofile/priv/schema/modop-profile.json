{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LCARS Modop Profile (strict, v2.5)",
  "description": "Modop fragment validated at load time. Reserved keys (apiVersion, kind, metadata.containment, metadata.name) are forbidden — mitigates 'containment override par modop' (PoC-11).",
  "type": "object",
  "not": {
    "anyOf": [
      { "required": ["apiVersion"] },
      { "required": ["kind"] }
    ]
  },
  "properties": {
    "metadata": {
      "type": "object",
      "not": {
        "anyOf": [
          { "required": ["containment"] },
          { "required": ["name"] }
        ]
      }
    }
  }
}
