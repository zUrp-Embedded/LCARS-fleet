# Verdicts vérif — lot F-C085→105

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan : **7 PERCE-code · 3 PERCE-doc/cosmétique · 3 THEORIQUE · 8 DOCTRINE**.

| Finding | VERDICT | Justif + citation | verrou |
|---|---|---|---|
| F-C085 | THEORIQUE | `project_onboard.ex:129` `require_default_branch_main(full_name)` gâte AVANT clone ; full_name multi-segment échoue `default_branch/2` avant disque ; repo Gitea mono-niveau | — |
| F-C086 | PERCE | `scaffold.ex:24` contrat `{:error,{:scaffold_write,_,_}}` mais `File.mkdir_p!` bang `:27/45/59` → raise au lieu du tuple | `scaffold.ex:27/45/59` mkdir_p non-bang mappé — fail-closed |
| F-C087 | PERCE-cosmétique | `scaffold.ex:88/108-109` dates gelées `2026-06-14` générées à chaque onboard | seam clock — low |
| F-C088 | PERCE-doc | `chain_integration_test.exs:409/459` commentaire « ROUTE-COMMENT gravée par create_issue » vs labels (`:83-91`) | doc |
| F-C089 | DOCTRINE | `bus.ex:247` `@permit_empty_default true` fail-open, intentionnel documenté ; readiness expose vide en :degraded (`readiness.ex:88`). Basculer prod = design | — |
| F-C090 | PERCE-doc | `test_helper.exs:2` « exclusion retirée » vs `:3` `exclude:[:r1_seam]` (×2). Périmé | doc |
| F-C091 | DOCTRINE | `emitter.ex:50-52` dispatch_action toujours `:ok` (broadcast jeté, silent) ; observabilité vs escalade load-bearing = design | — |
| F-C092 | PERCE | `emitter.ex:122-123` normalize_payload from_struct + maps inchangées → `"message"` imbriqué clés atom vs enveloppe string. Pas de forme-fil canonique | `emitter.ex:122` stringify clés imbriquées — parse-au-bord |
| F-C093 | DOCTRINE | `coord_backend.ex:40` défaut NotWiredYet log+`:ok` ; Cat5Escalator traite `:ok` routé. Optionnel vs load-bearing = design ; readiness :degraded (`readiness.ex:105`) | — |
| F-C094 | THEORIQUE | `cat5_escalator.ex:100-106` source-inconnue→`:ok`, mais seul appelant DriftMonitor passe sources figées (`drift_monitor.ex:78/93/101`) | — |
| F-C095 | DOCTRINE | `drift_monitor.ex:150-152` drift_count défaut 0 (pod.drift DORMANT) ; `cat5_escalator.ex:69` accepte toute map. Constructeurs par-source = design | — |
| F-C096 | THEORIQUE | `decision.ex:16` struct présence-clés, mais `Gatekeeper.validate/1` valide decision-v1.json (enum+reason) AVANT le struct (`gatekeeper.ex:60-61`) ; invalide = struct forgé | — |
| F-C097 | PERCE | `application.ex:66/70/78/82/96/100` knobs boot `if get_env` brut ; truthy non-bool démarre l'enfant, nil désactive → config malformée change topologie boot | `application.ex:66..100` lecteur booléen fail-loud — parse-au-bord |
| F-C098 | PERCE-latent | `audit_log.ex:46` `Jason.encode!` avant gestion erreur ; contrat `:ok\|{:error}` (`:40`) ; payload non-encodable → EncodeError. Caveat : 0 producteur live non-encodable | `audit_log.ex:46` `Jason.encode/1`+`{:error,{:encode_failed}}` — fail-closed |
| F-C099 | PERCE | `audit_log.ex:103-104` `:audit_log_max_bytes` brut ; `:81` `size>=max` ; 0/négatif→rotation permanente, string→ordre termes→jamais rotation | `audit_log.ex:103` parser entier positif fail-loud — parse-au-bord |
| F-C100 | PERCE-low | `audit_consumer.ex:48-50` matche tout `source::task_queue` incrémente ; catch-all `:154` `_→:ok` sans log → type inconnu compté « audité » | `audit_consumer.ex:48/154` guard known-type — source-match |
| F-C101 | DOCTRINE | `audit_consumer.ex:143/171/179/109` champs absents→`"?"`/nil. Placeholder vs signal invalid séparé = classification | — |
| F-C102 | DOCTRINE | `boot_orchestrator.ex:82` `run/1` toujours `:ok`, émission `fleet.boot_*` jetée. Observabilité vs load-bearing = classification | — |
| F-C103 | DOCTRINE | `mcp_monitor.ex:91-94` émet crashed sur :ok→:crashed seulement, best-effort jeté ; `:crashed→:crashed` no-op (`:99`) supprime récurrence. État requêtable (`:check_now`). Design | — |
| F-C104 | PERCE | `mcp_monitor.ex:158/162`+`mcp_watcher.ex:176/180/184` interval/target/fetcher bruts ; interval malformé crashe init ; fetcher non-fn retombe sur fetch Hex.pm réel. Config (B5) | lecteurs config fermés fail-loud — parse-au-bord |
| F-C105 | DOCTRINE | `shutdown.ex:241/259` défaut NoOpDispatcher → drain immédiat sous wiring absent ; readiness :degraded (`readiness.ex:136`). Fail-loud prod = design | — |

## Notes vérifie-le-vérificateur (worker)
- PERCE mécaniques nets (parse-au-bord) : F-C086,092,097,099,104 + F-C098/100 (avec caveat de portée).
- THEORIQUE — vérificateur en amont : F-C085 (default_branch gâte avant disque), F-C094 (sources figées), F-C096 (Gatekeeper.validate valide l'enum avant le struct).
- DOCTRINE best-effort-vs-load-bearing (B3, décision de classification) : F-C089,091,093,095,101,102,103,105 — plusieurs DÉJÀ surfacés :degraded par `Fleet.API.Readiness` (l'anti-hollow-green existe ; reste le fail-loud au chemin d'appel).
