# Verdicts vérif — lot F-C106→126

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan lot : **8 PERCE · 13 DOCTRINE · 0 THEORIQUE · 0 DEJA-FIXE**.

| Finding | VERDICT | Justif + citation | (si PERCE) vrai emplacement + verrou |
|---|---|---|---|
| F-C106 | PERCE | `shutdown.ex:291`+`:295` `{:reply,:ok,...}` alors que `do_wait_drain` produit `%{status: :timeout}` (`:314`) → timeout drain rendu `:ok` | `shutdown.ex:291/295` → typed-return `{:ok,:drained}`/`{:error,{:timeout,n}}` |
| F-C107 | PERCE | `shutdown.ex:264` `grace_ms=Keyword.get` puis `:265` `+5_000` (ArithmeticError si non-int) ; `:282` backend sans check behaviour | `shutdown.ex:264/282` → parse positive-int + valider callbacks à init |
| F-C108 | DOCTRINE | `application.ex:12` « no longer emitted » mais `step_run_consumer.ex:720` émet `workflow_map.failed` (mon Q2). Split live/inert | — |
| F-C109 | DOCTRINE | `loader.ex:95-101` normalize jette metadata (`:92` « deliberately discarded ») ; schéma la porte inerte | — |
| F-C110 | DOCTRINE | `gates.ex:32-33` severity knobs « out of scope » ; `eval_terminal_string` `:126-138` ignore. Implémenter ou retirer | — |
| F-C111 | DOCTRINE | `gatekeeper.ex:6` « pipe » vs `:49` `@issue_id "permanent-gatekeeper"` + `:110` « permanent ». Figer le modèle | — |
| F-C112 | DOCTRINE | `ensure_booted/1` `:81-82` `{:ok,pod_id}` sur is_binary ; présence≠liveness DÉJÀ documenté `:65-66` + `reboot/1` `:96`. Split d'API | — |
| F-C113 | PERCE | `gatekeeper.ex:98` `_ = killer.(@pod_id)` jeté ; killer `{:error,_}` → reboot succès, `do_spawn:129` ré-adopte `:already_started` | `gatekeeper.ex:98` → matcher le résultat killer (fail-closed holder-kill) |
| F-C114 | PERCE | `deliverable.ex:135` `check_keys(opts.identity)` sans is_map → BadMapError ; `:222` push? = truthy ; `git.ex:132` exact-match `%{push?: true}` ; author/msg non typés | `deliverable.ex:135/222` + `git.ex:132` → smart-ctor parse-au-bord |
| F-C115 | PERCE | `git.ex:286` `push/3` public + `:313-314` force_push si NFF ; invariant « system-owned branch » en prose (`:15-17`) pas dans le type ; force `HEAD:main` possible | `git.ex:313-314` → refspec system-owned OU force derrière fun privée du rebase |
| F-C116 | PERCE | `deliverable.ex:79` `Map.get(opts,:coauthor_role)`→nil en git_native ; `deliverable_gate.ex:63` `maybe_check_trailer(_,_,nil)→:ok` → publie sans `Co-authored-by` | `deliverable.ex:133-140` → exiger `:coauthor_role` pour `mode: :git_native` (champ requis) |
| F-C117 | PERCE | `git.ex:373/379` `git_push_timeout_ms`/`git_local_timeout_ms` bruts passés `timeout_ms:` sans parse | `git.ex:373/379` → parse positive-int fail-loud |
| F-C118 | DOCTRINE | `rest.ex:57-67` `200` + listes vides hard-codées (`:65-66`), moduledoc `:11` « MVP stubs ». Câbler read-models vs 501 | — |
| F-C119 | PERCE | `spawn_admission.ex:117` `Map.take([...,"issue_id"])` sans preuve binaire ; `publish_consumer.ex:100` `to_string(issue_id)` → number/liste JSON = id arbitraire. Chemin public POST /api/admin/spawn | `spawn_admission.ex:117` → rejeter issue_id non-binaire à l'admission (parse-au-bord ingress) |
| F-C120 | DOCTRINE | `build_info.ex:108` `{:ok, parse(content)}` sur tout fichier ; `:202-205` défauts. Parse « total » VOULU (`:102-103`). Provenance | — |
| F-C121 | DOCTRINE | `ws.ex:81` runtime déjà canonique (tuple legacy retiré) ; résiduel = test seam `:r1_seam` périmé. Archéologie | — |
| F-C122 | DOCTRINE | `application.ex:71` émet si NOTIFY_SOCKET `/` ; abstract socket `:86-89` `_→:ok` no-op. Edge niche | — |
| F-C123 | DOCTRINE | `observation/application.ex:10-11` doc HMAC/dashboard-Python périmée vs `rest.ex:22/173`. MAJ doc | — |
| F-C124 | DOCTRINE | `read_model.ex:100` `empty()` rescue (`:89` « INDISTINGUISHABLE from quiet-healthy ») ; `deck.ex:88` 200. Ajouter status/subscribed? | — |
| F-C125 | DOCTRINE | `deck.ex:154-155` `{:error,_}→[]` → `/table` vide « succès ». État table typé | — |
| F-C126 | DOCTRINE | `deck.ex:164-165` hard-coded `monk`/`archivist` prefix, `:159-163` « Temporary ». Champ cap-profile sémantique | — |

## Notes vérifie-le-vérificateur (worker)
- **F-C119** : `to_string` sur une map JSON lève `Protocol.UndefinedError` capté par le rescue de `publish_consumer.ex:49`→spawn.failed ; MAIS number/liste passe → brèche réelle pour ces formes. Vrai verrou = amont, à l'admission.
- **F-C115** : `non_fast_forward?/1` (`git.ex:358-362`) matche NFF/fetch-first et EXCLUT déjà hook/protected → force sur branche protégée serveur non-atteignable ; brèche restante = force sur branche partagée légitimement divergente (main).
- **C107/C114/C117** : même racine B5 (config/opts brutes sans parse positif-int) ; atteignables via config/opts malformés, pas attaquant réseau direct (contrairement à C119 = ingress HTTP public).
