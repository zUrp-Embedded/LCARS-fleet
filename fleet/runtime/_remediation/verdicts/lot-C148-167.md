# Verdicts vérif — lot F-C148→167

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan : **10 PERCE (4 doc + 6 script/gate/code) · 5 THEORIQUE · 5 DOCTRINE**.

**Fait structurel clé (gouverne le cluster modop-bundle)** : le SP pod prod = base rôle + `agent_draft` (`pod.ex:269` `SPBuilder.compose(cap, [], …)` liste modop VIDE ; `assets.ex:71`). Les `modop-bundles/*/sp.md` ne sont JAMAIS assemblés dans un prompt pod live → les claims « contradiction atteint un pod » sont non-atteignables (F-C149/150/155).

| Finding | VERDICT | Justif + citation | verrou |
|---|---|---|---|
| F-C148 | PERCE-doc | `brainstorming/sp.md:18` « FORBIDDEN NO EXCEPTIONS » vs `:130` « L0-L1 triviales skipped ». Auto-contradiction canon actif | scope NO-EXCEPTIONS à L2+ — correction-doc |
| F-C149 | THEORIQUE | `engineer.yaml:105 default:[fire-mode]` n'atteint jamais un pod (`pod.ex:269 compose(cap,[])`) ; pas d'invariant fire→lifetime | — |
| F-C150 | THEORIQUE | `consultant.yaml:78 [fire,archive]` combo invalide représentable mais G24-6 (`invariants.ex:147-149`) ne check que paires déclarées, prose non-assemblée | — |
| F-C151 | PERCE-doc | `subagent-spec-reviewer.md:23` qualifier=spec-compliance vs draft actif `agent-qualifier-base.md:78-79` qualifier=test-proof | réécrire vers sp_drafts + drift test — source-match |
| F-C152 | DOCTRINE | `subagent-implementer.md:12` injecte `dev` one-shot vs `engineer.yaml:85-86` « VIOLERAIT G24-11 ». Retirer/construire = design | — |
| F-C153 | DOCTRINE | `monk.ex:50` root défaut `monks/` + `catalog.ex:128` scanne `monks/*` (« INERT scaffolding »). Décision root Memory-X | — |
| F-C154 | THEORIQUE | `_frozen-monks/` NON scanné (`catalog.ex:128` scanne `monks/`) ; `Statut:actif`/`boot_at_start:true` inertes | — |
| F-C155 | THEORIQUE | `[archive,fire]` gelé doublement dormant (`_frozen-monks` unscanné + prose non-assemblée) | — |
| F-C156 | DOCTRINE | `monk.yaml:49 modop_set:[]`, `:37 /local/LCARS-v1.5`, non-v2.5 ; aucun code ne charge runtime-root priv/canon. Relocate/convert = design | — |
| F-C157 | DOCTRINE | alpha/beta définis 2× rosters divergents (`_frozen-monks` vs `priv/canon/fleets`), aucun chargé. Choisir roster = design | — |
| F-C158 | PERCE-doc | `coord-policies-v1.json:5` « 05_data-canon/config/… » alors qu'il valide `priv/config/…` | nommer priv/config — correction-doc |
| F-C159 | DOCTRINE | `profile` dual-sens : schéma `workflow-map-v2.5.json` string libre ; `gk-smoke.yaml profile:noop`/`poc-helloworld profile:fire-mode` (modop). Field-semantics = design | — |
| F-C160 | PERCE | `brief-gate` EST la map défaut prod (`step_dispatcher.ex:17`), or conformance couvre standard-qa/audit-only seulement (`loader_v25_test.exs:21,29`) | `modops_consumption_test.exs:48` couverture inventory-driven — gate-réel |
| F-C161 | PERCE | `gate-decision-v1.json:6 required:[decision,reason]` mais `verdict.ex:50-55 gate_decision/1` accepte sur `decision` seul (jamais reason/schéma). Atteignable par tout verdict juge sans reason | `verdict.ex:50` enforcer reason→halt_invalid OU relax schéma — fail-closed/parse-au-bord |
| F-C162 | PERCE-doc | `DESIGN-observabilite.md:123` « API auth HMAC » vs `api.ex:11-12`/`rest.ex:22` « HMAC REMOVED no-auth » | correction-doc |
| F-C163 | THEORIQUE | `.run-journal/run-global.md:5 Statut:actif` mais 0 consommateur runtime (grep vide). Misread humain, pas d'état resumable | — |
| F-C164 | PERCE | `install.sh:50` copie launchers mais PAS `claude_launch.identity`, or `publish-to-github.sh:44` VENDOR_IDENTITY défaut+`:78` exit si illisible (fichier existe en source) | `install.sh:50` ajouter claude_launch.identity — Bats check |
| F-C165 | PERCE | `provision-role-tokens.sh:46 ROLES="...reviewer vulcan"` mint `vulcan` non-canon, omet `starfleet` ; canon = `...reviewer starfleet` (`conformance_test.exs:18`, `starfleet.yaml:11` actif) | `provision-role-tokens.sh:46` dériver du canon cap-profile — source-match |
| F-C166 | PERCE | `gate-r0.1-bwrap.sh:15 HOST_SECRET=$HOME/.claude/.credentials.json` ; `:30-33` `cat` pod raté = PASS sans jamais asserter que le secret EXISTE côté host → vert sur machine sans fichier | `gate-r0.1-bwrap.sh:29` sentinelle host / précondition — fail-closed/gate-réel |
| F-C167 | PERCE-HIGH | Hollow green : `check_app` défini `gate-r0.8-canon.sh:18` mais JAMAIS appelé ; commentaire (`:33-36`) puis `FAIL=0`→`exit 0` (`:39-40`) | `gate-r0.8-canon.sh:33` restaurer appels check_app + assert ≥1 lancé — gate-réel |

## Notes vérifie-le-vérificateur (worker)
- Cluster modop-bundle (C149/150/155) pointe une prose que l'assemblage SP prod ne charge JAMAIS (`pod.ex:269`) → prémisse « atteint un pod » fausse.
- F-C160 : `brief-gate` n'est pas juste « active-looking », c'est la map défaut prod RÉELLE (`step_dispatcher.ex:17`) → renforce (vrai trou de couverture).
- F-C164/165/166/167 = les plus haut-confidence (défauts script/gate réels) ; F-C167 = hollow-gate high-sévérité.
