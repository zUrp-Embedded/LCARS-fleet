# Verdicts vérif — lot F-C043→063

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan : **8 PERCE · 2 THEORIQUE · 11 DOCTRINE**.

| Finding | VERDICT | Justif + citation | verrou |
|---|---|---|---|
| F-C043 | PERCE | `permanent_boot.ex:274-280` base présente-mais-invalide → session fraîche sans warning (`base_seed_uuid` nil silencieux sauf unreadable `:292-311`). Chaque boot permanent | `permanent_boot.ex:274` split :absent/:present_invalid — fail-closed |
| F-C044 | PERCE | `publish_consumer.ex:63` seul le rescue émet spawn.failed ; cap-profile err → warn only (`:111-114`), spawn_pod err → warn only (`:104-108`) = « 202 queued, 0 pod, 0 alarme » | `publish_consumer.ex:104,111` émettre spawn.failed — fail-closed |
| F-C045 | DOCTRINE | `seed_store.ex:128-129` toute erreur → `:none`. Split absent/corrompu = design | — |
| F-C046 | DOCTRINE | Doc retry re-emit (`work_items.ex:57-60`) vs item :completed persisté (`server.ex:294-300`)→double_submit. Align vs replay = design (forge+poll backstop) | — |
| F-C047 | PERCE | `delegation.ex:159` `"delivered" = issue closed` ; tout close (marker onboard/manuel) lu delivered ; `:153-158` admet la conflation | `delegation.ex:159` require merge proof — source-match/fail-closed |
| F-C048 | DOCTRINE | `supervisor.ex:121-132` socket-scan rescue→0→:operational (aveugle) ; auteur a choisi 0+warn vs faux :degraded. Nouveau statut :unknown = design | — |
| F-C049 | DOCTRINE | `fleet_mcp/mix.exs:39/62` dep event_router vestigiale (`:30-38` admet, gardée boot-order #576). Remove/keep = topologie | — |
| F-C050 | DOCTRINE | `reconciliation.ex:232-236` tout pod_status non-nil (incl :completed) = actif ; `:104-114` documente le wedge concurrent, différé. Tension state-split | — |
| F-C051 | DOCTRINE | `step_run_consumer.ex:703-711` workflow_map.failed/audit.verdict « Q2 DRAFT best-effort ». Décision politique fiabilité | — |
| F-C052 | DOCTRINE | `step_run_consumer.ex:416-423` payload nu garde repo/remote config ; prod boot repo=nil (`:195-199`)→fail-late-nil pas mis-write. Explicite = design | — |
| F-C053 | PERCE | `step_run_consumer.ex:596-600` CapProfile.load err → `"payload"` → producer? false (`gate_engine.ex:150-151`) → `{:judge}` (`step_run_build.ex:130-131`). Profil cassé reclassé juge | `step_run_consumer.ex:596-600` typed error propagé — fail-closed |
| F-C054 | THEORIQUE | `issue_id.ex:19` stringify tout, mais callers réels passent entiers forge (`spawn.ex:112,208`) | — |
| F-C055 | PERCE | `pod_id.ex:89-101` slug non-injectif : `owner/repo-a` et `owner-repo/a` → même `owner-repo-a`. Noms hyphénés réels collisionnent → pod_id/lock partagés | `pod_id.ex:89-101` clé repo réversible/hashée — smart-ctor |
| F-C056 | DOCTRINE | `roles.ex:21-56` accessors renvoient opt/config brut. Valider-au-boundary = design (mord sur misconfig) | — |
| F-C057 | THEORIQUE | `step_run_completer.ex:328-335` producer_of nil→"engineer", mais seul caller `:496` lit producer_branch toujours set (`step_run_build.ex:107,129`) ou fail-loud `:no_producer_branch` | — |
| F-C058 | DOCTRINE | `step_run_completer.ex:391` `_ = set_stage` jeté vs `labels.ex:1-9` « forge state machine ». Load-bearing vs dashboard = design | — |
| F-C059 | PERCE | `spawn.ex:351-360` safe_pod_info `:error` pour absence ET raise ; `:337-338` `:error→:dead→:ok` autorise reset. Raise transitoire sur pipe vivant → reset à froid (incohérent avec pod_alive? fail-closed) | `spawn.ex:342-360` split :absent/{:unknown} → {:skipped,:role_busy} — fail-closed |
| F-C060 | PERCE | `review_lifecycle.ex:228-235` `_ = unlock` jeté puis `{:ok,{:merged}}` ; le jumeau `route(:promote)` traite unlock en `with` load-bearing (`step_run_completer.ex:496-508`). Brique mergée reste lcars-in-flight | `review_lifecycle.ex:228` unlock load-bearing/typed — fail-closed / source-match |
| F-C061 | DOCTRINE | `role_dispatch.ex:87-93` rôle vide OU err cap-profile → `{:skipped,:no_role}`. Séparer humain-étranger/LCARS-role = design | — |
| F-C062 | PERCE | `arch_escalation.ex:148-162` add_label fail → log+`:ok` ; `escalate_rework:82` `{:skipped,escalated}` inconditionnel. lcars-awaits-arch = throttle poller → re-dispatch chaque tick | `arch_escalation.ex:148` propager add_label fail → typed :82 — fail-closed |
| F-C063 | DOCTRINE | `step_dispatcher.ex:449-450` default_workflow_map get_env `"brief-gate"` (config unset). Require-config-au-boot vs soft-default = design | — |

## Notes vérifie-le-vérificateur (worker)
- F-C054/F-C057 = pattern writer/reader-authority que l'auditeur adore, mais le vrai chemin lave (entiers forge ; producer_branch toujours set en amont ou fail-loud) → THEORIQUE.
- F-C060 vs F-C050 : `promote_pr` jette l'unlock ET la reconciliation ne scanne que les issues locked SANS PR ouverte (`reconciliation.ex:120-128`) → une brique mergée+closed encore lcars-in-flight n'est PAS re-dérivable par le backstop → renforce F-C060.
- Cluster PERCE F-C053/059/060/062 = même famille verrou : fail-closed sur load/probe/write fail au lieu de laver en voie bénigne.
