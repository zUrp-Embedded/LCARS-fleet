# Verdicts vérif — lot F-C022→042

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan : **7 PERCE · 5 THEORIQUE · 9 DOCTRINE**.
Discriminateur du worker : schéma FERME le champ (enum/rejeté→THEORIQUE) vs LAISSE OUVERT (string libre→PERCE) ; `Application.get_env` (opérateur) malformé → DOCTRINE.

| Finding | VERDICT | Justif + citation | verrou |
|---|---|---|---|
| F-C022 | PERCE | Hash couvre `sp_role_base`/modop (`sp_builder.ex:131`) mais pod écrit `sp_md <> agent_draft` (`pod.ex:282`), `agent_draft`=`agent-<role>-base.md` (`assets.ex:71`) jamais hashé → éditer un draft change le prompt pas la sha | hash à l'assemblage final `pod.ex:282` — source-match |
| F-C023 | DOCTRINE | `sp-map.yaml:8` architect « NOT here, historical draft ». Source-of-truth SP = design | — |
| F-C024 | THEORIQUE | `monk.ex:54` pas de guard under-root, mais seuls `_frozen-monks/*` (dormant, basenames propres) ont monk_registry ; profils actifs `:not_a_monk` | — |
| F-C025 | THEORIQUE | `blocks.ex:28` role_map YAML brut consommé SEULEMENT par mix task build-time (`:49`) ; runtime lit le `agent-<role>-base.md` pré-généré | — |
| F-C026 | DOCTRINE | `protocole-user-worker.md:4` « draft POC » injecté verbatim (`assets.ex:96`). Vocab périmé = contenu | — |
| F-C027 | PERCE | `scaffold.ex:82-85` `clone_or_skip(...,[])` opts vides → `phase.ex:80` slug défaut `"work"` ; le `slug:` dispatcher (`step_dispatcher.ex:199`) jamais threadé → clone initial toujours `feature/work` | thread `Keyword.take(state.opts,[:slug])` `scaffold.ex:85` |
| F-C028 | PERCE | `phase.ex:189` `pin_base_sha(_,sha) when sha in [nil,""]→{:ok,{"",0}}` fail-open vs `:172` reset fail-closed ; schéma permet `project.repo_path` statique (`cap-profile-v2.5.json:157`) sans base_sha → clone tip mouvant | require base_sha non-vide si repo_path `phase.ex:110` — fail-closed |
| F-C029 | PERCE | `phase.ex:252` `project["work_branch"]`→`git clone --branch` sans validation, alors que base/feature via `Fleet.GitRef.valid?` (`:99-100`) ; schéma `work_branch` string libre (`cap-profile-v2.5.json:161`) | valider work_branch via GitRef `phase.ex:229/252` — parse-au-bord |
| F-C030 | THEORIQUE | `completed_payload.ex:80` branche `{workflow_map_id,step}` 0 producteur ; consumer skip (`step_run_consumer.ex:394`). Contrat mort | — |
| F-C031 | PERCE | `launch_spec.ex:223-224` accepte tout string `:`, `hd`+join `" "` ; shell `bwrap_launch.sh:200` word-split → `"foo bar:skill"`=2 tokens ; `skills[]` string ouvert (`cap-profile-v2.5.json:80`) | parse slug plugin, reject whitespace/path `launch_spec.ex:223` — smart-ctor |
| F-C032 | THEORIQUE | `launch_spec.ex:199-207` fallback ro/default, mais schéma ferme mount `mode` enum [ro,rw] (`:36`) + permission_mode ABSENT (additionalProperties:false→rejeté). Fallback = struct bypass | — |
| F-C033 | DOCTRINE | `scaffold.ex:104-108` cp CLAUDE.md fail→warn+`:ok` (délibéré `:98` « degradation not HALT »). Design | — |
| F-C034 | DOCTRINE | `launch_env.ex:193` `:claude_dir` override ignore `human` ; app config, breach = multi-human deploy. Override test-only = design | — |
| F-C035 | PERCE | `brief.ex:101-109` `:unknown` (broker down)→warn+`:ok` → `:projecting` with ne fail pas → brief admin.spawn droppé, pod idle. Pas de backstop | `{:error,{:brief_slot_unknown,pod_id}}` `brief.ex:109` — fail-closed |
| F-C036 | DOCTRINE | `mcp_provision.ex:179` `is_map(spec)` puis `spec["args"]||[]`. `:mcp_server_spec` well-formed `runtime.exs:200`, malformé = operator-config only | — |
| F-C037 | PERCE | `pod.ex:522` `pod_has_active_task?` où `task_probe.ex:72` `:error` (broker down)→false → `:keep_state` consomme la deadline tirée (`:520`, non ré-armée) → watchdog neutralisé sur pod silencieux | 3-state (comme brief_slot) `pod.ex:521`/`task_probe.ex:71` — fail-closed |
| F-C038 | DOCTRINE | `state_fs.ex:188` write fail→error+`:ok` (4 sites, non-fatal blanket `:144-147`). Split per-transition = design | — |
| F-C039 | THEORIQUE | `paths.ex:126`/`liveness.ex:188` normalisent scope inconnu, mais schéma ferme `lifetime_scope` enum+required (`cap-profile-v2.5.json:97`) → bypass requis | — |
| F-C040 | DOCTRINE | `README.md:38` catalogue env vs template vide + `runtime.exs:462-475` 3 knobs seulement. Doc-surface = design | — |
| F-C041 | DOCTRINE | `launch_backend.ex:65` get_env sans ensure_loaded avant `.launch/2` (MCP seam gardé lui). App config operator-only. Parity = design | — |
| F-C042 | DOCTRINE | `spawner.ex:271` safe_clear_for_pod `:ok` après release fail loggé ; backstop poller documenté (`:244-246`). Classifier = design | — |

## Notes vérifie-le-vérificateur (worker)
- F-C029/031/028 « percent » car le SCHÉMA laisse le champ ouvert (work_branch, skills[], repo_path statique) ; vrai verrou = au load cap-profile ou au seam git/env, pas la ligne pointée.
- F-C037 : vraie conflation un niveau au-dessus du handler pointé → `task_probe.ex:72` (`:error`→false) ; le module a DÉJÀ la forme 3-state (`brief_slot/1`), le fix = la réutiliser.
