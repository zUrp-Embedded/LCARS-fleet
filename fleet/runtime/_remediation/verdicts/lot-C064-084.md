# Verdicts vérif — lot F-C064→084

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan : **10 PERCE-code · 4 PERCE-doc · 3 THEORIQUE · 4 DOCTRINE**.

| Finding | VERDICT | Justif + citation | verrou |
|---|---|---|---|
| F-C064 | DOCTRINE | `naming.ex:63-64` `rem(id,10000)` DEBT assumé (« repo 10000 collide repo 0 »). Widen/reject = design | — |
| F-C065 | THEORIQUE | `forge_protocol.ex:39` feature_branch invalide seulement pour n≤0/role vide ; callers réels n>0/role non-vide (`step_run_build.ex:129`) | — |
| F-C066 | PERCE | `gatekeeper_seal.ex:107-124` close_issue `{:error}` loggé, valeur jetée, `seal_and_merge`→`:ok` ; commentaire « NOT harmless » (churn) | `gatekeeper_seal.ex:107` typed partial/error — fail-closed |
| F-C067 | PERCE-doc | `gatekeeper_seal.ex:34-35` doc « fallback system token » vs `:59-63` fail-closed `:role_token_unavailable` | doc |
| F-C068 | PERCE | `forge_client.ex:675-676` post_route wfmap puis set_stage ; 2e write fail → wfmap half persiste ; `get_route:710-712` `_→:none` → dispatcher overwrite | `forge_client.ex:710` typed `{:route_half_state}` — fail-closed |
| F-C069 | PERCE | `jury.ex:81/148/179` 2xx non-liste → facts vides (verdicts/reviewers/rounds) ; load-bearing merge/rework, incohérent avec paginate fail-loud | `jury.ex:81,148,179` typed `{:unexpected_page_shape}` — parse-au-bord |
| F-C070 | PERCE-doc | `repo.ex:189` doc « fallback random UUID » vs `naming.ex:60-62` « NEVER fabricate UUID, absent→fail-loud » | doc |
| F-C071 | PERCE-doc | `forge_client.ex:32` doc « GET+PUT label » vs `:831` POST by name | doc |
| F-C072 | PERCE-doc | `forge_client.ex:109` doc « 50/page single page » vs `:147` paginate | doc |
| F-C073 | PERCE | `incident_registry.ex:150-151` `_ = write_wal` jeté, `{:reply,:ok}` ; write_wal `{:error}` (`:293-295`) ; contrat `:recorded`=mémoire+WAL (`:72`) | `incident_registry.ex:150` propager `{:error}` — fail-closed |
| F-C074 | PERCE | `incident_registry.ex:231/304/308` forge-down/corrupt→`%{}` silencieux ; moduledoc « fail-LOUD never loss » (`:14-15`) ; corrupt 2xx→empty+overwrite perd mémoire cross-machine | `:231/304/308` distinguer :not_found/read-fail — source-match/fail-closed |
| F-C075 | PERCE | `escalation.ex:69-80` discovery-label fail loggé, `escalate`→`{:ok,number}` ; contrat error_system = signal discovery durable (`:13`) | `escalation.ex:69` typed `{:escalation_unlabeled}` — fail-closed (résiduel doctrine : assignee-only OK ?) |
| F-C076 | PERCE | `escalation.ex:88-90` retry SANS assignee sur TOUTE erreur 1er call (pas de classif vs « account-absent » `:84-85`) → erreur transitoire/auth drop assignee | `escalation.ex:88` classifier avant fallback — source-match |
| F-C077 | THEORIQUE | Producteur émet toujours pod_id binaire (`pod.ex:692-693,601-602`) ; no-op catch-all (`incident_consumer.ex:112`) requiert breach producteur | — |
| F-C078 | THEORIQUE | `escalation.ex:101-119` spec atom() mais 4 clauses sans catch-all → atom invalide déjà fail-loud (function-clause) ; callers réels atoms figés | — |
| F-C079 | PERCE | `wake_recovery.ex:68-79` note_fun fail loggé, `re_wake`→`:ok` ; note `{:error,:registry_unavailable}` (`incident_registry.ex:62`) → ancre récurrence absente silencieuse (classe C073) | `wake_recovery.ex:68` typed error — fail-closed |
| F-C080 | DOCTRINE | `wake_recovery.ex:57` `_ = respawn_fun` jeté, mais le re-wake `:63` est la vraie autorité (pod reachable). Brancher sur respawn = tradeoff design | — |
| F-C081 | DOCTRINE | `worktree_sync.ex:74` clé par last-segment = modèle identité system-wide (comme `naming.ex:26`). Collision cross-owner = choix ; trailing-slash pas atteint (repos well-formed) | — |
| F-C082 | PERCE | `write_spacing.ex:23-24` config non-int-positif → gap disable silencieux ; low (ordre display), misconfig-only | `write_spacing.ex:22` validate closed duration — parse-au-bord |
| F-C083 | DOCTRINE | `brief_builder.ex:98/202/267` forge-read fail → briefs vides/génériques (documenté anti-wedge). Quels briefs requièrent quels facts = design | — |
| F-C084 | PERCE | `project_onboard.ex:232` create_repo 409→`{:ok,org/name}` puis clone/scaffold/push (`:83-95`) ; `refute_existing:220` File.exists? LOCAL seulement → repo forge existant sans dossier local muté | `project_onboard.ex:232/82` `already_exists` = état distinct non-onboardé (require import) — fail-closed |

## Notes vérifie-le-vérificateur (worker)
- Classe « preuve durable manquante rapportée succès » (doctrine fail-closed campagne) : F-C066,073,074,075,079 tous PERCE, verrou typed-partial/fail-closed au point cité.
- Doc-only (code correct, texte périmé) : F-C067,070,071,072.
- Catches reachability : F-C065/077/078 meurent à l'atteignabilité (writer/spec plus large que le domaine, mais callers réels feed valide). F-C080 : respawn jeté réel mais re-wake = autorité correcte, pas de faux succès.
- F-C060 vs F-C050 (cross-lot) : brique mergée+closed encore lcars-in-flight PAS re-dérivable par la reconciliation (ne scanne que locked-sans-PR) → renforce F-C060.
