# Verdicts vérif — lot F-C127→147

**Date** : 2026-07-10
**Dernière révision** : 2026-07-10
**Statut** : verdicts worker (verify-sweep phase 0) — à consolider dans LEDGER.csv
**Référencé par** : `../LEDGER.csv`

Bilan lot : **6 PERCE (5 doc-fix + 1 schéma) · 14 DOCTRINE · 1 DEJA-FIXE · 0 THEORIQUE**.

| Finding | VERDICT | Justif + citation | (si PERCE) verrou |
|---|---|---|---|
| F-C127 | DOCTRINE | `.credo.exs:208-212` 5 checks `disabled:`. Activer vs doc = outillage | — |
| F-C128 | PERCE | `.dialyzer_ignore.exs:1-3` en-tête « deps tierces » mais entrée `publish_consumer.ex` = notre source | correction-doc |
| F-C129 | DOCTRINE | Sobelow `only:[:dev]` (`mix.exs:149`), pas dans l'alias `gate`. Ajouter = design | — |
| F-C130 | DEJA-FIXE | `fleet_spawner/mix.exs:36` vraie dep app event_router → OTP ordonne par graphe, pas par liste ; invariant-ordre scopé aux paires sans-dep-compile | — |
| F-C131 | PERCE | `README.md:13` « 15 OTP apps » vs 14 dirs | correction-doc |
| F-C132 | PERCE | `etc/README.md:58` `/etc` visible + `sandbox_notrace_test.sh` modèle retiré vs `bwrap_launch.sh:264-322` binds sélectifs | correction-doc + source-match test |
| F-C133 | DOCTRINE | `archivist.yaml:37` ref `/local/LCARS-v1.5/sp/` ; `Fleet.Instance.Loader` inexistant. Séparer canon actif/archivé | — |
| F-C134 | PERCE | `fleet_workflow/README.md:24` namespace `Fleet.Workflow.GitRef` faux ; réel `fleet_cap_profile/.../git_ref.ex` | correction-doc |
| F-C135 | DOCTRINE | `bwrap_launch.sh:179` guard GIT_MIRROR + `:73-75` « décision user 2026-07-07 : garde+annote, inerte plus tard ». Différé | — |
| F-C136 | DOCTRINE | `claude_launch.sh:279-282` WARN sans fail sur `.mcp-fleet.json` (« content-agnostic assumé »). Fail-closed = design | — |
| F-C137 | PERCE | `host_launch.sh:34` doc 4-args inline-SP + `host_launch_test.sh:88` ; launcher strict 3-args `claude_launch.sh:59` | correction-doc + source-match test |
| F-C138 | DOCTRINE | Bridge Python sans `import_project` vs `pod_tools.ex:129`. Catalogue cross-langage main-sync = SSOT archi | — |
| F-C139 | DOCTRINE | Fixture `mcp_submit_server.py:37` `get_task` legacy vs central `get_work_item` ; gate inc4 non câblé. Archiver/réécrire | — |
| F-C140 | DOCTRINE | `inc3b2.sh:14` test inexistant ; `inc3b3.sh:34` superseded ; gate-r4-mcp-boot transport http superseded ; non câblés `shell_gate.sh`. Archiver | — |
| F-C141 | PERCE | `claude_launch.sh:182` `jq allowedTools \|\| exit 1` mais schéma ne requiert PAS allowedTools dans scope ; profil sans → mort au launch | **champ requis schéma** (cap-profile-v2.5.json scope) |
| F-C142 | DOCTRINE | `architect.yaml:47-49` n'autorise pas `import_project` vs `pod_tools.ex:129`. Actif→ajoute / dormant→retire = design | — |
| F-C143 | DOCTRINE | `deliverable_mode` défaut `payload` INTENTIONNEL (`cap-profile-v2.5.json:167` + `cap_profile.ex:406`), contraste voulu avec brief_kind. Requérir = design | — |
| F-C144 | DOCTRINE | 2 `noop` modop `{}` (cap-profiles/modop vs canon/modop). Dédoublonner/owner = design | — |
| F-C145 | DOCTRINE | Overlay actif stub vide `modop/tdd/profile.yaml:2` (« effet = sp.md »). Schéma SP-only = design | — |
| F-C146 | DOCTRINE | `pod.ex:269` `compose(cap, [], …)` → bundles modop non câblés (`sp_builder.ex:311`). Owner = design | — |
| F-C147 | DOCTRINE | `subagent-templates/` non lu (composeur `sp_builder.ex:113-114`) ; `qualifier.yaml:64` champ validé mais .md non consommés. Câbler/démoter = design | — |

## Notes vérifie-le-vérificateur (worker)
- **F-C130** : finding se croit atteignable, mais dep app réelle `fleet_spawner→event_router` (`mix.exs:36`) neutralise l'ordre-liste (OTP ordonne par graphe). Localisation fausse → DEJA-FIXE.
- **F-C141 vs F-C143** : même forme (« schéma ne requiert pas X »), verdicts OPPOSÉS. allowedTools dur-requis par un consommateur `exit 1` → PERCE. deliverable_mode a un défaut code intentionnel documenté → DOCTRINE.
- **F-C135/F-C136** : brèches réelles+atteignables MAIS chaque fix = alternative de design que le code assume/consigne déjà → DOCTRINE.
