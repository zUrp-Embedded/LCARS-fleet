{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Gate Decision (gatekeeper, projet)",
  "description": "Décision rendue par le gatekeeper sur une gate de pipeline projet. (Son ancien jumeau decision-v1.json — le schéma d'escalade du rail décision — est mort avec la brouette du 2026-08-19 ; celui-ci est le SEUL schéma de décision restant.) Vocab canon gatekeeper-exception.md.",
  "type": "object",
  "required": [
    "decision",
    "reason"
  ],
  "properties": {
    "decision": {
      "type": "string",
      "enum": [
        "continue",
        "abandon",
        "redirect",
        "escalate_user",
        "halt_wait_input"
      ]
    },
    "reason": {
      "type": "string",
      "minLength": 1
    },
    "details": {
      "type": "object"
    },
    "chain": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
