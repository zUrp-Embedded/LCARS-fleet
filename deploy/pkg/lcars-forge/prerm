#!/bin/bash
# SOURCE: deploy/pkg/lcars-forge/prerm
# AUTHOR: bob
# STARDATE: 2026-09-05
# STATUS: maintainer script dpkg — `lcars-forge` avant le retrait : dit ce que le postrm fera.
#         Rien de destructif ici.
#
# dpkg l'appelle : prerm remove · prerm upgrade · prerm deconfigure · prerm failed-upgrade
set -euo pipefail

say() { echo "lcars-forge/prerm: $*" >&2; }
case "${1:-}" in
  remove) ;;
  *) exit 0 ;;
esac
say "au retrait, les conteneurs de la forge et du runner seront ARRETES et retires ; leurs VOLUMES restent"
say "  (ils portent les depots). « apt purge lcars-forge » retire aussi les volumes, et le dit. Rien ici."
exit 0
