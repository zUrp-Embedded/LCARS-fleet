# Plancher ruff joué par test/shell_gate.sh : syntaxe et pyflakes, rien de stylistique.
[tool.ruff]
exclude = ["vendor", "_build", "deps", "tmp"]
line-length = 100

[tool.ruff.lint]
select = ["E9", "F"]
