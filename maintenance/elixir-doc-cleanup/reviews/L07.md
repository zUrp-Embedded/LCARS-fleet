# L07 — Identités, slots et graines de session

Douze fichiers relus, onze modifiés ; BootEpoch reste intact. Prose : 673 → 262 lignes ;
fichiers : 2 236 → 1 823. Bornes UID/repo, dépendance au format UUID accepté par le vendor,
risques de collision, sérialisation allocation/Registry et survivants sur disque conservés.
Conservation des identités builder/live distinctes, graines par ticket, témoins vendor et
commande de rafraîchissement, limites de fsync, budgets de lecture et protections contre les liens.

Corrections documentaires : SessionMint consulte l’OS et réserve repo 0 au rôle d’index 0 ;
alive_fun renvoie un booléen ; restore utilise le cwd courant ; les contrôles de liens ne sont
pas atomiques ; une ligne géante est matérialisée avant le budget ; les lignes JSON malformées
du sidecar sont ignorées. Aucun comportement, titre de test, fixture ou diagnostic modifié.

Relecture indépendante gpt-5.6-terra, medium : aucun constat matériel.
Validation : douze AST inchangés hors documentation/positions, formatage, diff check,
75 tests ciblés réussis et 75 contrats réussis. Les tests de permissions tournent sous
l’utilisateur vanille ; leur réserve sur les UID privilégiés reste documentée.
Base et périmètre : ../base-commit.txt et lignes L07 de ../batches.tsv.
