# L25 — Heuristiques et types de conflits

Huit fichiers relus et modifiés. Prose : 119 → 93 lignes ; fichiers : 803 → 772.
La faible réduction reflète les précisions utiles : les chaînes citées ne sont pas analysées
par un lexer ; un seul token de forme volatile suffit ; le ratio utilise deux tokenisations
et un dénominateur pris sur ours. La présence de base augmente certains scores sans comparaison
de son contenu. Les dates sont triées lexicalement sans validation de calendrier/fuseau ;
les versions ignorent notamment l'ordre des identifiants de prerelease.

Les docs séparent ces diagnostics des types autorisés en écriture. Les commentaires de
résolution directe renvoient aux préférences d'Assemble et aux gardes de Conflict. Le score
n'est pas probabiliste ; les annotations ne le modifient pas, et deux dimensions numériques
ne sont pas restituées dans sa map. Les structs manuels ne garantissent pas les relations
entre champs ; les traces sont des données dont les consommateurs assurent la persistance.

Relecture indépendante gpt-5.6-terra, medium : aucun constat matériel.
Validation : huit AST inchangés hors documentation/positions, formatage et diff check ;
39 tests du domaine et 75 contrats réussis. Aucun changement de logique ou chaîne opérationnelle.
Base et périmètre : ../base-commit.txt et lignes L25 de ../batches.tsv.
