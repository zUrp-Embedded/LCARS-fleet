# L20 — Blocs, images et sections de prompts

Onze fichiers relus, dix modifiés ; comportement Composer conservé. Prose : 617 → 272 lignes ;
fichiers : 2 201 → 1 856. Conservation des priorités métier/système, des identifiants runtime
à préserver dans les traductions, du marqueur de génération et de la protection des drafts
manuels par l'audit séparé. generate! ne lance pas cet audit ; ses écritures sont destructives
et non transactionnelles. confined? sélectionne la destination sans valider les segments.

Image décrit les lectures de contenu et de fingerprints séparées, les publications successives
sans rollback global, les sources masquées incluses dans les empreintes, les fichiers exactement
vides refusés et les noms empruntés non résolus par le contrôle de déclaration. drift ne compare
que les chemins enregistrés de l'image par défaut, sans détecter de nouveaux fichiers ; republish
restaure une map non validée à la racine courante. Ces constats ne changent pas le fonctionnement.

Monk distingue validation du conteneur et validation absente des entrées/chemins. RepoSections
conserve les raisons du retrait par section et de la notice sans extrait, sans promettre qu'un
filtre lexical prouve l'innocuité. Son parseur ne comprend pas les clôtures de blocs de code.

Les tests de parité comparent des hash de fragments et des CLAUDE.md, pas tous les prompts finaux.
Les fixtures d'image décrivent les substitutions directes et fallbacks réellement exercés ;
les invariants de dialecte restent des contrôles lexicaux sur une population partielle.
Les cinq tests Monk restent explicitement désactivés avec leurs fixtures historiques.

Relecture indépendante gpt-5.6-terra, medium : aucun constat matériel.
Validation : onze AST inchangés hors documentation/positions, formatage et diff check ;
51 tests ciblés réussis, 5 préexistants ignorés, 75 contrats réussis.
Base et périmètre : ../base-commit.txt et lignes L20 de ../batches.tsv.
