# L02 — Cycle de vie, tmux et nettoyage des pods

Douze fichiers relus, dix modifiés ; `Pod.Fs` et `Pod.Paths` conservés.
Prose : 837 → 339 lignes ; fichiers : 4 709 → 4 213 lignes.

Conservés : limites des opérations tmux, pattern de kill, preuve socket,
grâce du warden, distinction indisponible/vide, alarmes de dispatch, raisons
des fixtures et barrières de synchronisation. Retrait des récits historiques
et paraphrases d’assertions. `confirm_dead?/2` est décrit selon le code : le
premier résultat explicite `:absent` suffit, sans confirmation répétée.

Validation : 12 AST inchangés hors documentation/positions ; formatage et
`git diff --check` réussis ; 150 tests ciblés réussis avant/après L02.
Les 75 contrats passent avec L02–L04 présents. Relecture indépendante
`gpt-5.6-terra`, medium, lecture seule : aucun constat matériel.

Base et périmètre : `../base-commit.txt` et lignes L02 de `../batches.tsv`.
Reproduction AST : `elixir maintenance/elixir-doc-cleanup/verify_lot.exs L02`.
