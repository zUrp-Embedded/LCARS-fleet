# L27 — Client Forge et son harnais

Deux fichiers relus et modifiés. Prose : 819 → 456 lignes ; fichiers : 3 875 → 3 511.
Conservation des exports de seams MCP, de la dépendance Boundary sans @behaviour inverse,
des raisons métier des fermetures/retraits, du corps repo (pas name) pour les dépendances,
des refs/SHA pour les sondes et des règles distinctes de merge/auto-merge.

Les contrats décrivent les branches effectives : pas d'idempotence générale ni de transaction,
sources de credentials ordonnées, dédup système/rôle/tout auteur, pagination sans snapshot,
marqueurs observables sans preuve de livraison. La fermeture peut réussir avec stamp ou
retrait de lock échoué ; un rôle sans credential est ignoré par le compteur, même si cette
indisponibilité ne prouve pas son inexistence. Le cache de login est un couple empreinte/login
par URL et peut alterner entre rôles. Les valeurs structurées ne sont pas toutes validées.

Les docs CI distinguent absence de vote, warning, skipped et groupes d'id non ordonnables.
La fusion décrit le contrôle mergeable puis le retry textuel, trois tentatives au total et
le nettoyage séparé dont les erreurs retournées n'annulent pas le merge. Les labels sont
vérifiés par nom, sans garantie de couleur/description ni exclusivité vérifiée localement.

Les tests gardent toutes leurs fixtures et assertions. Plusieurs limites utiles sont rendues
explicites : verdicts_of force :unscoped ; assertions de map partielle tolèrent des verdicts
supplémentaires ; pages servies par ordre d'appel sans query vérifiée ; méthode override non
inspectée ; stamp non vérifiable ; absence de second appel non mesurée ; cache partagé et
transport Plug ne prouvant pas le décodage distant. Les vrais contrôles discriminants demeurent.

Relecture indépendante gpt-5.6-terra, medium : aucun constat matériel.
Validation : deux AST inchangés hors documentation/positions, formatage et diff check ;
146 tests ciblés et 75 contrats réussis. Aucun changement de logique, fixture ou chaîne opérationnelle.
Base et périmètre : ../base-commit.txt et lignes L27 de ../batches.tsv.
