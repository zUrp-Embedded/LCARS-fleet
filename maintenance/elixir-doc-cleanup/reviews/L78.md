# L78 — Tests des controles de corpus

Deux fichiers relus et modifies. Prose : 114 → 24 lignes ; fichiers : 455 → 364.
Conservation des exclusions de decouverte, de la topologie runtime/deploy et des
raisons du format des fixtures. Les portes renvoient des listings sans executer les
temoins. Le stub deploy est sous runtime et n'est pas exerce comme porte deploy.
La comparaison de refute porte sur du texte normalise, pas le comportement.

Relecture independante gpt-5.6-terra medium : aucun constat introduit. Le titre de
test preexistant affirmant comparer le comportement est conserve dans le perimetre
gele des noms de tests ; le moduledoc en precise la limite.
Validation : deux AST inchanges hors documentation/positions, formatage et diff check ;
25 tests et 75 contrats reussis.
Base et perimetre : ../base-commit.txt et lignes L78 de ../batches.tsv.
