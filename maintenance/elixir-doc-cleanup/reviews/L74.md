# L74 — Controles runtime et leurs temoins

Six fichiers relus et modifies. Prose : 704 → 147 lignes ; fichiers : 2 918 → 2 360.
Conservation des prerequisites de spawn, de l'annulation native des state_timeout,
du role d'exception du gatekeeper et du contexte de catalogue requis au chargement.
Les controles de presence ne sont plus presentes comme preuves de chemins executes.

awaits-arch/in-flight compare des formes dans un fichier ; le controle eval/export
indexe le fichier entier par nom de module et ignore l'arite. Le controle stdout lit
un corps eval ou le fichier contenant une tache Mix, sans ordre ni atteignabilite.
Bare aliases ignore la portee lexicale et la validite des cibles ; Loader reconnait
trois orthographes sans resoudre les alias ni valider le contenu des options.
Les fixtures et assertions restent intactes ; leurs docs distinguent controle de
source et execution. Relecture independante gpt-5.6-terra medium : aucun constat
materiel. Validation : six AST inchanges hors documentation/positions, formatage et
diff check ; 376 tests Mix et 75 contrats reussis. Aucun comportement modifie.
Base et perimetre : ../base-commit.txt et lignes L74 de ../batches.tsv.
