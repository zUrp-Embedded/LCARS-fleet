# L67 — Deck et projection d'observation

Sept fichiers relus et modifies. Prose : 353 → 110 lignes ; fichiers : 1 396 → 1 196.
Conservation de la source installee des medias, du role distinct de son icone,
des exclusions Memory-X, du catalogue illisible distinct du vide et du statut ETS.
La regression CSS conserve son mecanisme : piste reservee par un parent reel pour
un enfant absent. L'inventaire reste une heuristique textuelle, pas une execution DOM.

Limites explicitees : prefixe de chemin lexical sans resolution des symlinks ;
valeurs JSON partiellement validees ; snapshots projection/statut distincts ;
abonnement memorise sans suivi de perte ulterieure. Le shell conserve un panneau
gatekeeper sans projection correspondante. Les strings HTML/JS restent inchangees,
y compris leurs commentaires ; le nettoyage porte sur la documentation Elixir.
Relecture independante gpt-5.6-terra medium : aucun constat materiel.
Validation : sept AST inchanges hors documentation/positions, formatage et diff check ;
35 tests et 75 contrats reussis. Aucun comportement modifie.
Base et perimetre : ../base-commit.txt et lignes L67 de ../batches.tsv.
