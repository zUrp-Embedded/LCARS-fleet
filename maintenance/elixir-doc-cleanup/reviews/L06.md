# L06 — Projection MCP, workspace et transcripts

Six fichiers relus et modifiés. Prose : 281 → 112 lignes ; fichiers : 808 → 639.
Conservation des chemins host/namespace distincts,
identité par socket, quoting shell, vérification des liens et limite check-then-read,
CLAUDE.md suivi intact, exclusion Git locale de la copie générée et `.dead` forensique.
Récits d’extraction et répétitions supprimés. Le GC des JSONL est décrit comme un
renommage plutôt qu’un effacement. Aucun prompt ni fixture opérationnelle modifié.

Relecture indépendante `gpt-5.6-terra`, medium : résumé MCP trop général ; corrigé
en explicitant le cas StubBackend sans spec et le refus pour les vrais backends.
Validation : six AST inchangés hors documentation/positions, formatage et diff check,
9 tests ciblés et 75 contrats réussis avec L05/L06 présents. La correction issue
de revue ne modifie que le résumé module.
Base et périmètre : `../base-commit.txt`, lignes L06 de `../batches.tsv`.
